experiment_name: "fashionmnist_hybrid_qcnn_svm_cpu_safe"
binary_classes: [3, 8]

dataset:
  name: "fashion_mnist"
  binary_classes: [3, 8]
  grayscale: true
  root: "./data"

training:
  batch_size: 64
  epochs: 40
  learning_rate: 1.0e-3
  kfold: 3
  early_stopping: 10

scheduler: null

svm:
  n_trials: 30

pennylane:
  device: "lightning.qubit"
  workers: 0
  tile_size: 64
  kernel_centering: true
