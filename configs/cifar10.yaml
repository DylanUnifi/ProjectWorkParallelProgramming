experiment_name: "svm_qkernel_cifar10_final_float64"

dataset:
  name: cifar10
  binary_classes: [3, 8]     # Cat vs Ship
  grayscale: true
  root: ./data

pennylane:
  layers: 2

svm:
  n_trials: 30

cv_folds: 3