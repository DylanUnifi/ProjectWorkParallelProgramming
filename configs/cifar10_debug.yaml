experiment_name: "svm_qkernel_cifar10_debug"
binary_classes: [3, 5]

dataset:
  name: "cifar10"
  binary_classes: [3, 5]
  grayscale: false
  root: "./data"

svm:
  n_trials: 10

pennylane:
  device: "lightning.qubit"
  workers: 0
  tile_size: 32
  kernel_centering: true
